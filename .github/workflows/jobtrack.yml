name: JobTrack CI

# Build, verify formatting and run the fast core test suite on every push
# and pull request. Mirrors the local commit gate (see CLAUDE.md and
# scripts/fast-test.sh) but runs in --strict mode, since a real budget
# overrun should fail CI even though it only warns locally.
on:
  push:
  pull_request:

# Cancel superseded runs on the same ref to save CI minutes.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  DOTNET_NOLOGO: 'true'
  DOTNET_CLI_TELEMETRY_OPTOUT: 'true'
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 'true'

jobs:
  build-test:
    name: Build, format & test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore JobTrack.slnx

      - name: Build
        run: dotnet build JobTrack.slnx --no-restore

      # - name: Verify formatting
      #   run: dotnet format JobTrack.slnx --verify-no-changes

      - name: Fast core tests
        run: scripts/fast-test.sh --strict
