@page
@model AchievementModel
@{
	ViewData["Title"] = "Redirecting";
}
@* GET always redirects to /Jobs/Work#status (see AchievementModel) -- this markup never renders in
   normal operation. It exists only so the page type has a view. *@
